name: Pull Request Check

on: 
  pull_request:
    branches: [ main ]

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        run: |
         echo Test
         exit 0
